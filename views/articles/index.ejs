<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
      integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm"
      crossorigin="anonymous"
    />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>BLOG</title>
  </head>
  <body>
    <div class="conatiner">
      <!-- this is bootstrap class which will give padding around the text in each article -->
      <h1 class="container mb-4">Blog Articles</h1>
      <!--mb-4 gives margin space at bottom-->
      <div class="container">
        <a href="/articles/new" class="btn btn-success">New Article</a>
      </div>
      <!--new link is created on clicking on new articles. the class name has to be as put in dox of bootstrap-->
      <!--  to iterate through each article we dont use the "=" sign as in <%=    %>. Not having equal to will only  not display the output -->
      <!-- below code snippet is using Embedded JavaScript (EJS) templating to iterate over an array of articles and create a series of Bootstrap cards displaying the titles of the articles.-->
      <%articles.forEach(article => { %>
      <div class="container card mt-4">
        <div class="container card-body">
          <h4 class="container card-title"><%= article.title %></h4>
          <div class="container card-subtitle text-muted mb-2">
            <%=article.createdAt.toLocaleDateString()%>
            <!-- to locale date string converts the whole date timezone to a normal date format    -->
          </div>
          <div class="container card-text mb-2"><%= article.description %></div>
          <a href="articles/<%=article.slug%>" class="btn btn-primary ml-3">Read More
          </a>
          <a href="articles/edit/<%= article.id %>" class="btn btn-info">Edit</a
          >
          <form
            action="/articles/<%= article.id %>?_method=DELETE"
            method=" POST"
            class="d-inline"
          >
            <button type="submit" class="btn btn-danger">Delete</button>
          </form>
        </div>
      </div>
      <% }) %>
    </div>
  </body>
</html>
